import { WaitImageDirective } from './wait-image.directive';

describe('WaitImageDirective', () => {
  it('should create an instance', () => {
    const directive = new WaitImageDirective();
    expect(directive).toBeTruthy();
  });
});
